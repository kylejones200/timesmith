name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install deps
        run: |
          uv sync --frozen --group dev

      - name: Run tests with coverage (blocking)
        run: |
          uv run pytest -v --tb=short --maxfail=5 --durations=10 --cov=timesmith --cov-report=term-missing --cov-report=xml

      - name: Upload coverage to Codecov (optional)
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
          flags: unittests
          name: codecov-umbrella

      - name: Lint with ruff (blocking - critical issues only)
        run: |
          # Check for critical issues (errors, not warnings)
          # E501 is ignored in pyproject.toml (line length handled by formatter)
          uv run ruff check timesmith/ tests/ --select E,F --ignore E501
          # Format check (blocking)
          uv run ruff format --check timesmith/ tests/

      - name: Type check (non-blocking - informational only)
        continue-on-error: true
        run: |
          uv run mypy timesmith/ --show-error-codes || echo "Type check issues found (non-blocking - see mypy config)"
