repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.15
    hooks:
      - id: ruff
        # Quote "E,F" so YAML does not split into separate args (ruff would treat "F" as a path).
        args: [--select, "E,F", --ignore, E501, --fix]
      - id: ruff-format
